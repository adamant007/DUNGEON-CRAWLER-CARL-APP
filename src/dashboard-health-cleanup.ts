const STYLE='cc-dashboard-health-cleanup-style';
function clean(s:string){return s.replace(/\s+/g,' ').trim().toLowerCase()}
function activeTab(){const nav=document.querySelector('[aria-label="Primary navigation"]');const b=nav?.querySelector('button.active,[aria-current="page"]') as HTMLElement|null;return clean(b?.textContent||'')}
function hideDuplicateHealth(){const isDash=activeTab()==='dashboard';document.querySelectorAll<HTMLElement>('[data-cc-dashboard-health-hidden="1"]').forEach(el=>{if(!isDash){el.style.display='';el.removeAttribute('data-cc-dashboard-health-hidden')}});if(!isDash)return;const main=document.querySelector('.app>main');if(!main)return;const candidates=[...main.querySelectorAll<HTMLElement>('section,article,div')];for(const el of candidates){const txt=clean(el.innerText||'');if(!txt.startsWith('health'))continue;if(!txt.includes('take 5')||!txt.includes('heal 5'))continue;const nested=[...el.querySelectorAll<HTMLElement>('section,article,div')].some(x=>x!==el&&clean(x.innerText||'').startsWith('health')&&clean(x.innerText||'').includes('take 5')&&clean(x.innerText||'').includes('heal 5'));if(nested)continue;el.dataset.ccDashboardHealthHidden='1';el.style.display='none';break}}
function style(){if(document.getElementById(STYLE))return;const s=document.createElement('style');s.id=STYLE;s.textContent=`body[data-cc-primary-tab="dashboard"] #cc-mobile-resource-hud{display:none!important}`;document.head.appendChild(s)}
function apply(){style();hideDuplicateHealth()}
document.addEventListener('click',e=>{if((e.target as Element)?.closest('[aria-label="Primary navigation"],.character-bar'))setTimeout(apply,100)},true);window.addEventListener('cc:character-updated',()=>setTimeout(apply,60));window.addEventListener('cc:campaign-changed',()=>setTimeout(apply,60));if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(apply,800),{once:true});else setTimeout(apply,800);
export {apply as applyDashboardHealthCleanup};