/* Ginger Dragon Studios — stable landing handoff. No entrance cinematic. */
function lock(a:boolean){document.documentElement.classList.toggle('studio-landing-active',a);document.body.classList.toggle('studio-landing-active',a)}
function styles(){if(document.getElementById('gd-stable-handoff'))return;const s=document.createElement('style');s.id='gd-stable-handoff';s.textContent=`
.studio-stable-overlay{position:fixed;inset:0;z-index:2147483000;pointer-events:none;background:#050403;opacity:0;transition:opacity .18s ease}.studio-stable-overlay.go{opacity:1}.studio-stable-overlay.out{opacity:0}
`;document.head.appendChild(s)}
function install(){const src=document.querySelector<HTMLImageElement>('img.studio-hero-art');if(!src)return;if(src.dataset.posterEnhanced==='stable')return;styles();src.dataset.posterEnhanced='stable';lock(false);const candidates=Array.from(document.querySelectorAll<HTMLElement>('button,a,[role="button"]'));const enter=candidates.find(el=>/enter|crawler companion/i.test((el.textContent||'')+' '+(el.getAttribute('aria-label')||'')));if(!enter)return;let busy=false;enter.addEventListener('click',()=>{if(busy)return;busy=true;const o=document.createElement('div');o.className='studio-stable-overlay';document.body.appendChild(o);requestAnimationFrame(()=>o.classList.add('go'));setTimeout(()=>{o.classList.add('out');setTimeout(()=>o.remove(),190)},240)},true)}
function boot(){install();new MutationObserver(()=>install()).observe(document.documentElement,{childList:true,subtree:true})}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();